# 面试舱重构设计手册

## 1. 结论

面试舱不应继续按“页面卡片数量”扩展，而应重构为三个清晰区域：

- **用户端训练启动台**：只展示后端返回的可用组合，帮助学生开始一场可完成、可评分、可复盘的面试。
- **管理员题库治理台**：承载导入、校验、预览、发布、归档、恢复、重建索引和版本审计。
- **报告复盘面板**：解释本场追问策略、命中知识点数量和下一步复训方向，但不暴露题库版本号等治理信息。

这份手册以现有决策为准：采用 AI-Interview 的能力迁移，不引入第二套 Spring/Vue/MySQL/Qdrant 运行时；前端继续落在当前 React 工作台，后端继续由 Go + PostgreSQL 承接。

---

## 2. 已确认决策对前端的约束

### 2.1 普通用户侧

- 面试主链路优先使用新题库体系，旧 `InterviewQuestion` 只作为兼容/异常兜底，不用于补题量。
- 首期开场题按 `domain + difficulty + question_role in (opening, mixed) + status = published` 做简单可控筛选，不做复杂推荐排序。
- 首期只展示后端判定可用的 `category + difficulty` 组合；未开放组合不显示。
- 组合状态由后端根据题库实际状态返回，前端不得写死开放组合。
- 个人档案缺少简历摘要或项目经历摘要时，只提示“补充后更精准”，不得做警告、错误或阻断。
- 报告页可以展示追问策略摘要、命中知识点数量和建议补强方向，但普通用户不展示题库版本号、`sourceRef`、版本历史和审计信息。
- 索引失败时，开场题仍可走普通筛选，追问检索回退到现有规则链路；前端不应把索引失败表现为整场面试不可用。

### 2.2 管理员侧

- 首期题库管理只开放给 `admin`，不开放给 `instructor`。
- 题库导入只允许管理员手工导入结构化 JSON 内容包，不自动从案例工坊或 UGC 回流。
- 导入流程必须是 `校验 -> 试运行/预览 -> 正式发布`，预览阶段不写正式库。
- 每条题目必须包含 `principles + pitfalls + followUpPaths`，且数组、数量、顺序和内容质量都要通过硬校验。
- `category` 与 `domain` 使用受控枚举；`tags` 可以自由填写，但系统必须做 trim、去重和命名规范化。
- 单题状态至少为 `draft / published / archived`；`draft` 与 `archived` 不进入用户侧主链路。
- 已发布题目允许在线编辑，保存后直接生效，但必须填写 `change_note`、校验 `base_version`、写入版本历史，并把 `vector_status` 置为异步索引状态。
- 归档、恢复、编辑、批次导入都必须复用同一套版本写入契约。
- 管理端需要可见 `vector_status=failed`、`version_type=manual_edit/restore_archived`、最近编辑人、最近编辑时间、开放组合命中和关闭原因。

---

## 3. 面试舱的产品边界

### 3.1 面试舱收什么

面试舱只管理能被面试流程消费、并能产出结构化评价的题库资源。正式题库原子至少需要具备：

- `title`：前端和管理端展示标题。
- `subject`：稳定考察点标题，用于去重、归类、检索和统计，不能写成自然语言题干。
- `category / domain / difficulty`：受控分类与难度。
- `question_role`：`opening / followup / mixed`，其中 `mixed` 是例外类型，不是默认类型。
- `principles`：支撑一次完整回答的核心原理。
- `pitfalls`：常见误区，顺序表示优先级。
- `followUpPaths`：追问路径，至少 2 条，顺序表示默认追问优先级。
- `status / current_version / vector_status`：治理和运行状态。

### 3.2 面试舱不收什么

- **完整排障剧本**：如果核心交互是逐步询问线索、定位 root cause，应继续放在排查工坊。
- **社区长文原文**：应先进入案例工坊或人工清洗流程，不能自动进入面试题库。
- **纯知识笔记**：没有可评价问题、追问路径和评分维度时，只能作为素材。
- **一次性 AI 生成题**：可以进入候选池或导入包，但不能绕过校验、预览和管理员发布。
- **没有稳定 `id` 的内容**：无法支持版本历史、重复导入判断和会话追溯。

### 3.3 与排查工坊的边界

| 维度 | 面试舱 | 排查工坊 |
|---|---|---|
| 用户目标 | 练习面试表达、技术判断和岗位匹配度 | 训练故障定位、证据链和根因分析 |
| 核心对象 | `InterviewKnowledgeAtom` / 开场题快照 / 追问原子快照 | `ScenarioQuestion` / 线索 / 根因 |
| 交互方式 | 回答、追问、评分、报告 | 询问线索、判断根因、复盘 |
| 关键结构 | `principles`、`pitfalls`、`followUpPaths`、五维评分 | `root_cause`、`clues`、`standard_procedure` |
| 成功标准 | 回答质量与目标岗位匹配 | 根因定位与证据链完整 |
| 首期联动 | 不自动互转，只能人工导入结构化包 | 不自动反写面试题库 |

同一个素材可以人工衍生出两种训练资源，但必须显式转化：排查工坊版本围绕线索揭示，面试舱版本围绕候选人表达、取舍和追问。

---

## 4. 信息架构

### 4.1 用户端：训练启动台

目标是让学生快速回答三个问题：

- 我现在能练什么？
- 这个训练是否适合我的目标级别？
- 开始后会经历怎样的评分和追问？

首期建议结构：

1. **顶部任务区**
   - 当前目标岗位级别：来自用户档案或默认配置。
   - 当前可启动组合：由后端返回，不在前端写死。
   - 中性档案提示：简历摘要或项目经历为空时展示“补充后更精准”。
   - 主 CTA：开始面试。

2. **可启动轨道区**
   - 只渲染后端返回的 `available` 组合。
   - 每个轨道展示 `category`、`domain`、`difficulty`、题目角色覆盖、预计时长和简短说明。
   - 不展示未开放组合，避免“看得见但不能用”的噪音入口。
   - 启动失败时提示“题库状态已更新，请刷新后重试”或后端返回的可解释原因。

3. **评分与流程说明区**
   - 展示五维评分、最多三轮追问、动态追问和规则回退。
   - 强调“索引异常不影响完成面试”，降低用户误解。

4. **历史面试区**
   - 默认展示最近记录。
   - 后续支持按 `domain / difficulty / question_type / status` 过滤。
   - 历史项展示分数、轮次、报告入口和继续训练入口。
   - 普通用户不展示题库版本号。

### 4.2 管理端：题库治理台

管理入口放在现有 admin 区域，不新建独立后台；系统状态页只展示治理摘要，不承载完整管理操作。

首期页面分区：

1. **治理摘要**
   - 总题量、`published` 数、`draft` 数、`archived` 数。
   - `vector_status=failed` 数。
   - 开放组合数。
   - 近期导入批次和最近编辑时间。

2. **导入与预览**
   - 上传或粘贴标准 JSON 导入包。
   - `validate` 阶段展示结构错误、内容质量错误、重复 ID、非法枚举。
   - `dry-run / preview` 阶段展示新增、更新、归档、命中开放组合、`opening/followup/mixed` 数量、`error_count`、`warning_count`。
   - `publish` 阶段必须二次确认，并填写发布备注。

3. **题目列表**
   - 默认隐藏 `archived`，但提供状态筛选。
   - 支持 `category / domain / difficulty / question_role / vector_status / version_type` 筛选。
   - 展示标题、稳定考察点、当前版本、状态、索引状态、最近编辑人、最近编辑时间。

4. **单题详情与编辑**
   - 展示 `principles / pitfalls / followUpPaths` 原始结构化内容。
   - 展示该题进入开场题池、追问池或两者都可。
   - 展示命中的开放组合，以及未命中组合的原因。
   - 编辑已发布题必须携带 `base_version`，保存前二次确认：“保存后将立即影响后续新面试，历史会话不受影响”。
   - 普通编辑不混入 `draft / published / archived` 状态切换；归档和恢复走独立动作。

5. **版本历史**
   - 默认按 `created_at DESC` 排序。
   - 展示版本号、`version_type`、管理员、时间、`change_note`、`no_content_change`。
   - 版本详情展示完整 `snapshot` 和 `diff_summary`。
   - 首期不做回滚到历史版本。

### 4.3 报告复盘页

报告页不是治理页，重点是“用户下一步怎么练”。

建议增加：

- 本场追问策略摘要：深挖、补救、切换知识点或规则回退。
- 命中知识点数量：只展示数量和领域，不展示内部原文。
- 建议补强方向：映射到可启动训练轨道或学习计划。
- 检索回退说明：如果本场有回退，用用户可理解的话说明“本轮基于规则追问完成”。

不得展示：

- 题库版本号。
- `sourceRef`。
- 版本 diff。
- 管理员审计信息。
- 内部 prompt 或检索上下文原文。

---

## 5. 分类与组合模型

### 5.1 首期不要把所有东西塞进 `domain`

当前旧页面把领域目录、岗位级别说明和启动轨道都写在 `launchpadConfig.ts` 里。这个结构适合 MVP 演示，但扩库后会把三个职责混在一起：

- 目录分类：有哪些能力域。
- 题库治理：哪些组合满足题量和质量门槛。
- 启动轨道：用户当前可以开始哪些训练。

建议首期前端分清：

- `category`：少量稳定一级分组，用于开放组合和管理筛选，例如 `java / database / cache / ai_llm`。
- `domain`：面试域或能力域，继续使用后端受控枚举。
- `difficulty`：沿用 `L1-L5`，管理端可增加语义标签。
- `question_role`：决定开场题池、追问池或两者都可。
- `tags`：灵活补充检索维度，不能替代受控分类。
- `launch track`：后端返回的可启动组合视图，不等于题库分类本身。

### 5.2 首期开放组合

已确认首期优先开放：

- `java + L2/L3`
- `database + L2/L3`
- `cache + L2/L3`
- `ai_llm + L2/L3`

但前端仍不得把这些组合写死为最终真相。正确做法是：

- 后端根据题库状态、最低题量门槛和索引状态返回可见组合。
- 前端只消费后端返回的组合列表。
- 后端关闭组合后，普通用户侧自然不再展示。
- 管理端展示关闭原因和受影响字段。

### 5.3 最低题量门槛

每个 `category + difficulty` 组合至少满足：

- `opening / mixed` 题目 >= 3。
- `followup / mixed` 题目 >= 6。

达不到门槛时：

- 普通用户不显示该组合。
- 不使用旧 `InterviewQuestion` 补运营缺口。
- 管理端显示题量不足和关闭原因。

### 5.4 Topic 的位置

原文中的 `Topic` 仍有价值，但首期不应把 Topic 设计成新的主导航真相。建议定位为：

- 管理端题库覆盖和补题分析的次级维度。
- 报告页建议复训方向的可读标签。
- 后续覆盖热力图和学习路径的基础。

首期如果后端尚未稳定提供 Topic 聚合，用户端不应硬做 Topic 导航；可以先保留在设计文档和管理端扩展位。

---

## 6. 前端模块拆分建议

### 6.1 启动台配置拆分

当前 `frontend/src/features/interviews/launchpadConfig.ts` 同时承载领域目录、岗位级别、启动轨道和展示文案。建议后续拆成：

- `interviewTaxonomy.ts`
  - 只存前端展示所需的受控枚举标签、难度语义、图标和说明。
  - 不存“哪些组合可启动”的事实。
- `interviewLaunchTracks.ts`
  - 只作为 API 未接入前的临时 mock 或后端响应类型适配层。
  - 接入 API 后由后端组合接口驱动。
- `interviewCopy.ts`
  - 存页面说明、空状态、错误状态、确认弹窗文案。
- `interviewBankTypes.ts`
  - 存题库治理相关前端类型，避免散落在页面组件中。

### 6.2 状态管理边界

建议新增或扩展：

- `interviewSessionStore.ts`
  - 负责面试运行态、启动参数、当前会话、每轮追问摘要。
- `interviewBankStore.ts`
  - 负责管理员题库列表、批次预览、单题编辑、版本历史。

不要把题库管理状态塞进 `systemStore.ts`。系统状态页可以展示题库治理摘要，但不应承载题库导入、编辑和版本历史。

### 6.3 API 边界

用户端不应暴露复杂检索接口。前端仍只调用：

- 获取可启动组合。
- 创建面试会话。
- 提交回答。
- 获取报告。

管理端才调用：

- 导入校验。
- 预览。
- 发布。
- 归档。
- 恢复。
- 编辑。
- 版本历史。
- 重建索引。

---

## 7. 视觉与交互设计方向

### 7.1 视觉定位

当前面试舱页面已经形成深色“命令屏 / 雷达面板”风格：深海蓝背景、青绿色信号、玫红异常点、白色启动卡片。后续不应突然改成普通浅色 SaaS 卡片。

推荐方向：**Interview Signal Deck**。

设计关键词：

- 技术面试像一次信号扫描，不是营销落地页。
- 轨道卡片像可启动的训练频道。
- 评分维度像仪表读数。
- 异常和回退状态用少量玫红提示，但不制造焦虑。

### 7.2 设计 token 建议

色彩：

- `#020617`：主背景，保持当前深色基底。
- `#071827`：面板背景。
- `#5EEAD4`：可用、选中、主信号。
- `#7DD3FC`：结构标签和次级标题。
- `#FB7185`：异常、索引失败、风险提示。
- `#F8FAFC`：主文字。
- `#94A3B8`：次级说明文字。
- `#CCFBF1`：浅色启动卡片和高亮底色。

字体：

- 标题可采用有技术感的等宽或窄体风格，但必须克制使用。
- 正文保持高可读性，优先保证中文阅读密度。
- 数据、状态、版本号、枚举值使用等宽风格，以强化治理感。

签名元素：

- 使用“信号雷达条 / 组合健康矩阵”作为记忆点。
- 用户端只展示可用信号；管理端展示完整健康矩阵和失败原因。
- 不使用 emoji 作为图标，继续使用 Lucide 等一致 SVG 图标。

### 7.3 布局原则

- 桌面端采用 `启动区 + 轨道区 + 说明区 + 历史区`，核心 CTA 始终在首屏可见。
- 宽屏可使用双列，左侧轨道矩阵，右侧当前配置和评分说明。
- 平板以下收敛为单列，轨道卡片横向滚动或自然堆叠。
- 375px 移动端必须避免横向滚动，轨道卡片和历史项都改成单列。
- 页面缩放到 125% 和 150% 时，标题、按钮和状态标签不能互相覆盖。

### 7.4 交互状态

启动台必须覆盖：

- 加载态：正在读取可用组合。
- 空状态：当前没有可启动训练，说明原因并引导稍后重试或联系管理员。
- 错误态：获取组合失败、创建会话失败、题库状态变更导致启动失败。
- 降级态：追问检索回退到规则链路时，报告页用温和解释，不在启动页制造阻断。
- 键盘态：轨道选择支持方向键、Home、End 和可见 focus。
- 减少动效：遵守 `prefers-reduced-motion`。

管理员题库治理台还必须覆盖：

- 校验中。
- 预览无错误但有 warning。
- 部分发布。
- 索引 pending。
- 索引 failed。
- 版本冲突。
- 归档二次确认。
- 恢复失败。

---

## 8. 页面级验收标准

### 8.1 用户端启动台

- 只展示后端返回的可用组合。
- 未开放组合不显示。
- 能清楚说明本次面试的 `category / domain / difficulty / question_role`。
- 用户未填写简历摘要或项目摘要时，只出现中性提示。
- 创建会话失败时展示可操作错误，不吞掉后端原因。
- 历史面试默认可读，并能进入报告。
- 普通用户看不到题库版本号、`sourceRef`、审计字段和内部检索上下文。

### 8.2 管理端治理台

- 管理入口仅 `admin` 可见。
- 导入必须经过校验、预览和发布三段。
- 预览页能区分 error 与 warning，并展示新增、更新、归档、组合命中统计。
- 单题详情能看见原始结构化内容、题池归属、开放组合和未命中原因。
- 在线编辑已发布题必须校验 `base_version`，要求 `change_note`，并二次确认。
- 版本历史展示版本号、版本类型、操作者、时间、备注和 `no_content_change`。
- `archived` 默认隐藏但可筛选查看。
- 可筛选 `vector_status=failed` 并触发管理员重建索引。

### 8.3 报告页

- 展示追问策略摘要和建议补强方向。
- 展示命中知识点数量或领域摘要。
- 检索失败或弱召回时展示规则回退说明。
- 不展示治理信息。

---

## 9. 落地顺序

### Phase 1：启动台去硬编码

- 从 `launchpadConfig.ts` 拆出 taxonomy、copy 和临时 track 数据。
- 新增可启动组合 API 的前端适配层。
- 页面只渲染后端返回的组合。
- 保留当前视觉风格，不重做页面骨架。

### Phase 2：题库治理最小管理端

- 在 admin 区域新增 Interview Bank 管理入口。
- 实现导入校验、预览、发布、题目列表和状态筛选。
- 支持 `vector_status=failed` 筛选和重建索引入口。
- 系统状态页只展示题库治理摘要。

### Phase 3：单题编辑与版本历史

- 实现已发布题编辑。
- 实现 `base_version` 冲突提示。
- 实现版本历史列表和详情。
- 实现归档、恢复和二次确认。

### Phase 4：报告和历史增强

- 报告页展示追问策略摘要、命中知识点数量和建议补强方向。
- 历史面试增加筛选。
- 把报告建议回流到学习仪表盘。

### Phase 5：覆盖分析增强

- 管理端增加 Topic 或知识点覆盖热力图。
- 显示低覆盖组合、关闭原因和补题建议。
- 用户端仍只展示可启动组合，不展示未开放路线图。

---

## 10. 一句话原则

面试舱不是“题目列表”，而是“由题库治理驱动的可启动训练系统”：普通用户只看到能开始、能完成、能复盘的训练；管理员才看到题库状态、版本历史、索引失败和组合关闭原因。
