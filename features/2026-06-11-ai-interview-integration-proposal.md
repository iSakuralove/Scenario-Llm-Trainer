# AI-Interview 接入调研与方案文档

## 目标

调研 `nzy0510/AI-Interview` 仓库，结合当前项目现状，形成“如何接入”的讨论结论、需求文档和技术方案文档。

## 修改范围

- `docs/ai-interview-integration-prd.md`
- `docs/ai-interview-integration-tech-design.md`
- `features/2026-06-11-ai-interview-integration-proposal.md`

## 核心实现

- 阅读外部仓库的 README、CONTEXT、ADR、前后端入口与题库管理实现。
- 对照当前项目的面试舱、学习闭环、系统状态和存储架构，明确：
  - 哪些能力可复用
  - 哪些能力不应直接并入
  - 推荐的接入路径
- 新增 `CONTEXT.md`，把“能力迁移接入 / 运行时并入 / 面试开场题 / 面试知识原子”等关键术语固定下来。
- 新增 `docs/adr/0001-ai-interview-capability-migration.md`，固化“接入方式采用能力迁移，不做整仓运行时并入”的架构决策。
- 根据用户新增决策，进一步明确“个人简历/项目经历摘要”首期不是独立模块，也不是 PDF 解析流程，而是挂到现有“个人档案”中的可选字段。
- 根据用户新增决策，明确首期“面试题库管理”权限只给 `admin`，`instructor` 继续聚焦案例工坊初审。
- 根据用户新增修正，明确现有 `InterviewQuestion` 在首期不再担任主链路开场题，只作为新题库体系的兜底数据源。
- 根据用户新增确认，明确首期新题库采用同表存储，但必须使用 `question_role` 区分开场题、追问资源和兼用记录。
- 根据用户新增确认，明确首期开场题选择只做简单可控筛选，不引入复杂推荐算法。
- 根据用户新增确认，明确首期面试题库只允许 `admin` 手工导入结构化内容包，不自动从案例工坊/UGC 回流；并明确案例工坊与面试题库是两个独立模块。
- 根据用户新增确认，并参考外部 `AI-Interview` 仓库脚本，明确首期每条题目至少必须包含 `principles + pitfalls + followUpPaths` 三个核心块，缺一不可；本项目首期的数据门槛比外部仓库现状更严格。
- 根据用户新增确认，明确首期导入流程必须采用 `校验 -> 试运行/预览 -> 正式发布` 三段式，不允许上传后直接生效。
- 根据用户新增确认，明确首期试运行/预览只做纯模拟，不写正式题库、不改发布态、不触发索引写入。
- 根据用户新增确认，明确正式发布时同步触发当前批次的增量索引，并保留管理员手动重建索引入口；“可回滚的试导入”归入后续增强期。
- 根据用户新增确认，明确索引失败时必须降级：开场题普通筛选照常可用，追问检索回退到现有规则链路，不让整场面试不可用。
- 根据用户新增确认，明确首期底层难度继续沿用 `L1-L5`，管理端允许增加语义标签辅助理解；同时题库分类首期只允许少量受控枚举，不接受自由文本分类。
- 根据用户新增确认，明确首期 `tags` 允许管理员自由填写，但系统必须做基础规范化。
- 根据用户新增确认，明确首期保留 `question_role = mixed`，但它只作为例外类型，不作为默认录题方式。
- 根据用户新增确认，明确首期每个 `category + difficulty` 组合必须满足最低题量门槛，不满足则不直接开放主链路。
- 根据用户新增确认，明确题量不足的组合首期直接不开放，不用旧 `InterviewQuestion` 去补运营缺口。
- 根据用户新增确认，明确旧 `InterviewQuestion` 首期只用于真正的兼容/异常，不用于补题量、不用于补建设进度、不用于偷偷给用户兜底体验。
- 根据用户新增确认，明确一旦触发旧 `InterviewQuestion` 兼容兜底，状态只向管理员暴露，不向普通用户暴露。
- 根据用户新增确认，明确首期管理员侧对“兼容兜底触发”只做被动可见 + 可追踪，不强制实时处理。
- 根据用户新增确认，明确首期只开放少量高价值组合：`java/database/cache/ai_llm` 的 `L2/L3`。
- 根据用户新增修正，明确未开放组合在前端不显示，有题库且可用的组合才显示。
- 根据用户新增确认，明确首期组合状态由后端根据题库实际状态统一返回，不写死前端。
- 根据用户新增确认，明确当个人档案缺少简历摘要/项目经历摘要时，前端只给中性提示，不做错误、警告或阻断。
- 根据用户新增确认，明确导入包中的 `subject` 必须是稳定的“面试考察点标题”，不能直接写成自然语言题干。
- 根据用户新增确认，明确首期导入包同时保留 `title` 与 `subject`，两者职责分开：`subject` 做稳定锚点，`title` 做展示标题。
- 根据用户新增确认，明确首期允许 `title = subject`，不强制每条题目都写两套不同文案。
- 根据用户新增确认，明确首期 `followUpPaths` 至少 2 条，作为硬性数据门槛。
- 根据用户新增确认，明确首期 `principles` 必须足以支撑一次完整回答，不能只是关键词或占位提示。
- 根据用户新增确认，明确首期 `principles` 至少 2 条，作为硬性数据门槛。
- 根据用户新增确认，明确首期 `pitfalls` 至少 2 条，作为硬性数据门槛。
- 根据用户新增确认，明确首期 `pitfalls` 与 `followUpPaths` 必须保留录入顺序，顺序可表达优先级。
- 根据用户新增确认，明确同一稳定 `id` 的题目再次导入时，首期按更新旧题处理，并保留版本历史。
- 根据用户新增确认，明确即使同一稳定 `id` 内容完全一致，再次导入时也仍然生成新版本记录。
- 根据用户新增确认，明确版本历史必须区分 `content_update`、`duplicate_import`、`manual_edit`、`restore_archived` 四种类型。
- 根据用户新增确认，明确首期开放“已发布题目编辑入口”，且编辑保存后直接生效：保留版本历史，但当前版本立即切换。
- 根据用户新增确认，明确在线编辑保存后先返回保存成功，索引异步补齐，并通过 `vector_status` 暴露 `pending / failed / indexed` 状态。
- 根据用户新增确认，明确在线编辑已发布题目时必须复用导入链路的同一套硬校验规则，不能绕过字段、枚举和内容质量校验。
- 根据用户新增确认，明确在线编辑保存成功时管理员必须填写简短编辑原因 / 变更备注，并写入版本历史和审计信息。
- 根据用户新增确认，明确在线编辑生成的新版本类型为 `manual_edit`。
- 根据用户新增确认，明确在线编辑保存前需要轻量二次确认弹窗，但不引入复杂审批流。
- 根据用户新增确认，明确在线编辑已发布题目时不允许修改稳定 `id`；如需更换 `id`，应新建题目并归档旧题。
- 根据用户新增确认，明确在线编辑已发布题目时允许修改 `question_role`，但保存后必须立即重算题池归属与开放组合命中情况。
- 根据用户新增确认，明确在线编辑已发布题目时允许修改 `category / domain / difficulty`，但必须命中受控枚举，保存后立即重算开放组合。
- 根据用户新增确认，明确在线编辑已发布题目时允许修改 `subject`，但仍必须是稳定考察点标题，不能改成自然语言题干。
- 根据用户新增确认，明确在线编辑已发布题目时允许修改 `title`，且仍允许 `title = subject`。
- 根据用户新增确认，明确在线编辑已发布题目时允许修改 `sourceRef`，但必须非空，且只在管理端、版本历史和审计中可见。
- 根据用户新增确认，明确在线编辑已发布题目时允许修改 `principles / pitfalls / followUpPaths`，但必须满足数组、数量、顺序和内容质量硬校验。
- 根据用户新增确认，明确在线编辑普通表单不允许修改单题状态 `draft / published / archived`；状态变化继续走独立发布 / 归档动作。
- 根据用户新增确认，明确在线编辑保存时即使内容没有实际变化，也仍生成 `manual_edit` 版本，并在版本详情标记 `no_content_change = true`。
- 根据用户新增确认，明确版本历史需要保存字段级 diff 摘要，不保存复杂富文本 diff。
- 根据用户新增确认，明确在线编辑保存时必须校验版本号；管理员基于旧版本编辑时拒绝保存并提示刷新。
- 根据用户新增确认，明确版本历史单独建表，建议为 `interview_knowledge_atom_versions`，不塞进主表 JSON。
- 根据用户新增确认，明确首期审计信息与版本历史共表，不单独建审计表。
- 根据用户新增确认，明确题目主表只保存当前生效版本，版本表追历史。
- 根据用户新增确认，明确题目主表增加 `current_version`。
- 根据用户新增确认，明确在线编辑提交必须携带 `base_version`；不带或不匹配就拒绝保存。
- 根据用户新增确认，明确版本 diff 摘要字段使用 `JSONB`。
- 根据用户新增确认，明确归档、恢复、编辑复用同一套版本写入函数。
- 根据用户新增确认，明确批次导入生成的版本也写入同一版本表。
- 根据用户新增确认，明确 `current_version` 从 `1` 开始，且每次版本事件单调递增。
- 根据用户新增确认，明确 `duplicate_import` 即使内容不变，也推进 `current_version`。
- 根据用户新增确认，明确版本表 `snapshot` 保存完整标准化题目内容。
- 根据用户新增确认，明确 `diff_summary` 只用于摘要展示，不承担恢复能力。
- 根据用户新增确认，明确版本表需要索引 `(atom_id, version DESC)`。
- 根据用户新增确认，明确版本表需要按 `version_type / admin_id / created_at` 建索引。
- 根据用户新增确认，明确主表 `current_version` 与版本表最新版本不一致时视为数据异常。
- 根据用户新增确认，明确首期不做“回滚到历史版本”能力。
- 根据用户新增确认，明确 `snapshot` 保存完整标准化字段：`id / title / subject / domain / difficulty / category / question_role / sourceRef / tags / principles / pitfalls / followUpPaths / status`。
- 根据用户新增确认，明确 `snapshot` 不保存 `vector_status / last_indexed_at` 这类运行时索引字段。
- 根据用户新增确认，明确 `base_version` 冲突提示保持轻量：“版本已更新，请刷新后重试”。
- 根据用户新增确认，明确不做强制覆盖别人刚改内容的按钮。
- 根据用户新增确认，明确版本详情页显示 `no_content_change` 标记。
- 根据用户新增确认，明确版本历史列表默认按 `created_at DESC` 排序。
- 根据用户新增确认，明确版本历史列表展示版本号。
- 根据用户新增确认，明确首期不做批量在线编辑多题。
- 根据用户新增确认，明确在线编辑已发布题目时允许修改 `tags`，但保存时必须 trim、去重、统一大小写或别名映射。
- 根据用户新增确认，明确在线编辑允许把 `mixed` 改成 `opening` 或 `followup`，保存后立即重算题池和组合命中。
- 根据用户新增确认，明确在线编辑导致某个 `category + difficulty` 组合低于最低题量时，后端自动关闭该组合，普通用户不再看到。
- 根据用户新增确认，明确组合被自动关闭时只在管理端显示警告，不做消息推送。
- 根据用户新增确认，明确在线编辑版本历史和审计信息必须记录操作者 `admin_id`、操作时间、`change_note` 和版本类型。
- 根据用户新增确认，明确首期不允许物理删除题目，只允许归档，避免破坏版本历史、审计和会话追溯。
- 根据用户新增确认，明确归档题目必须填写归档原因。
- 根据用户新增确认，明确归档题目允许恢复为 `published`，但必须走独立恢复动作、硬校验和二次确认；恢复归档题不用填写恢复备注。
- 根据用户新增确认，明确恢复归档题时如果硬校验不通过，必须拒绝恢复。
- 根据用户新增确认，明确恢复归档题生成的新版本类型为 `restore_archived`。
- 根据用户新增确认，明确恢复归档题通过硬校验后立即恢复为 `published`，可进入后续新面试主链路。
- 根据用户新增确认，明确恢复归档题后索引异步期间允许做开场题，追问增强等待索引完成。
- 根据用户新增确认，明确 `archive_reason` 必须进入版本历史和审计信息。
- 根据用户新增确认，明确版本 diff 摘要只记录字段名和摘要，不记录完整旧正文。
- 根据用户新增确认，明确管理端题库列表默认隐藏 `archived`，但提供状态筛选查看。
- 根据用户新增确认，明确管理端需要支持按 `version_type = manual_edit / restore_archived` 筛选。
- 根据用户新增确认，明确题库列表需要显示“最近编辑人 / 最近编辑时间”。
- 根据用户新增确认，明确普通用户报告页不展示题库版本号，版本号只给管理端看。
- 根据用户新增确认，明确系统状态页展示题库治理摘要：总题量、`published` 数、`failed` 索引数、开放组合数。
- 根据用户新增确认，明确题库管理入口放到现有 `admin` 区域，不新建独立后台。
- 根据用户新增确认，明确首期不做用户自定义模型配置，继续沿用当前系统级 Provider。
- 根据用户新增确认，明确手动重建索引只允许 `admin` 操作。
- 根据用户新增确认，明确索引失败不影响开场题普通筛选，只影响追问检索增强。
- 根据用户新增确认，明确管理端需要支持筛选 `vector_status = failed` 的题目，方便集中修复。
- 根据用户新增确认，明确普通用户正在进行中的面试继续使用会话创建时的开场题快照，在线编辑只影响后续新面试。
- 根据用户新增确认，明确正在进行中的面试后续轮次追问检索使用编辑后的实时题库内容，但每轮命中的原子必须保存轻量快照。
- 根据用户新增确认，明确创建面试会话时必须保存开场题题目快照，避免后续改题污染历史会话与报告解释。
- 根据用户新增确认，明确每轮实际命中的追问知识原子也要保存轻量快照，但只保存元数据，不保存整段正文。
- 根据用户新增确认，明确同一个导入包里如果出现重复 `id`，首期直接判错，不自动取最后一条。
- 根据用户新增确认，明确当导入包中的 `category` 不在受控枚举里时，首期直接报错，不自动映射。
- 根据用户新增确认，明确导入包中的 `domain` 也采用受控枚举，不接受自由文本。
- 根据用户新增确认，明确 `sourceRef` 需要保留为可追溯字段，但首期不在前端用户界面显示。
- 根据用户新增确认，明确 `sourceRef` 首期必填且不能为空。
- 根据用户新增确认，明确首期 `sourceRef` 允许批次级共用顶层值，单题未填写时继承顶层值，但入库后每条题都必须有自己的 `sourceRef`。
- 根据用户新增确认，明确首期 `questionRole` 允许批次级默认值，单题未填写时继承顶层默认值，但单题可显式覆盖。
- 根据用户新增确认，明确首期 `difficulty` 也允许批次级默认值，单题未填写时继承，单题可显式覆盖。
- 根据用户新增确认，明确首期 `category` 也允许批次级默认值，单题未填写时继承，单题可显式覆盖，但仍必须命中受控枚举。
- 根据用户新增确认，明确首期 `tags` 也允许批次级默认值；单题显式填写时与顶层标签做去重合并。
- 根据用户新增确认，明确首期不允许 `principles / pitfalls / followUpPaths` 使用批次级默认值，必须逐题显式填写。
- 根据用户新增确认，明确首期导入包中 `principles / pitfalls / followUpPaths` 的最终规范统一为数组。
- 根据用户新增确认，明确即使结构字段齐全，只要内容明显重复、空泛或模板味过重，也要在首期校验阶段直接拦掉。
- 根据用户新增确认，明确首期内容质量校验采用“规则校验为主、管理员人工复核为辅”，AI 不作为主裁判。
- 根据用户新增确认，明确首期管理员人工复核时，关键发布动作必须填写简短发布备注 / 处理意见。
- 根据用户新增确认，明确首期允许批次内部分题目发布，不要求整批全量发布。
- 根据用户新增确认，明确首期批次状态需要细分，并至少包含 `partially_published`。
- 根据用户新增确认，明确首期必须同时保留批次状态和单题状态，不能只靠批次状态推断单题发布情况。
- 根据用户新增确认，明确首期单题状态至少包含 `draft / published / archived`。
- 根据用户新增确认，明确 `archived` 状态的题彻底退出主链路，但历史与版本仍保留。
- 根据用户新增确认，明确 `draft` 状态的题完全不进入任何用户侧面试链路，只在管理端可见。
- 根据用户新增确认，明确首期批次详情页必须显示每条题的 `category / domain / difficulty / question_role / 单题状态`。
- 根据用户新增确认，明确首期管理员点开单题时，必须能看到 `principles / pitfalls / followUpPaths` 的原始结构化内容。
- 根据用户新增确认，明确首期管理员在单题详情里，应直接看到这条题会进入哪个池子，而不只是看到 `question_role` 裸枚举值。
- 根据用户新增确认，明确首期管理员在单题详情里，还应看到这条题当前命中的开放组合。
- 根据用户新增确认，明确首期管理员在单题详情里，还应看到这条题为什么没有命中某些组合的原因。
- 根据用户新增确认，明确首期批次预览页要直接显示新增、更新、归档、命中开放组合等汇总统计。
- 根据用户新增确认，明确首期批次预览页要分开显示 `error_count` 与 `warning_count`，并提供各自明细入口。
- 根据用户新增确认，明确首期批次预览页要显示 opening / followup / mixed 的数量统计。
- 根据用户新增确认，明确首期批次预览页要单独统计因内容质量问题被拦截的题目数量。
- 根据用户新增确认，明确首期批次预览页要单独统计因结构字段缺失被拦截的题目数量。
- 根据用户新增确认，明确首期管理端最小动作集只保留：校验、试运行/预览、正式发布、归档、恢复归档、重建索引。
- 根据用户新增确认，明确首期“检索预览”并入“试运行 / 预览”，不单独算后台动作。
- 文档中给出分阶段落地建议，避免直接走双系统并存。

## 影响范围

- 仅新增方案文档，不改业务代码。

## 验证方式

- 外部仓库：阅读 `README.md`、`CONTEXT.md`、关键 controller/service、Question Bank Admin 组件和 ADR。
- 当前仓库：使用 CodeGraph 核对面试、学习闭环、存储与 AI 能力边界。

## 已知限制

- 当前文档基于仓库快照和现有代码理解，尚未进入实现阶段。
- 若后续你明确要求“双系统并存”或“外部子系统桥接”，文档需要改写。
