# 实施计划

1. RED：后端测试，最近低分会话产生 `source_kind=weak_dimension` 推荐。
2. GREEN：补 launchpad 推荐逻辑和 `recent_sessions` 弱项摘要。
3. 前端最近训练卡展示最低维度。
4. 回归 `go test ./internal/httpapi`、`npm --prefix frontend run lint/build`。
