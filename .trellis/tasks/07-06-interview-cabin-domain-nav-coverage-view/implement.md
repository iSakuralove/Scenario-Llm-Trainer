# 面试舱领域导航与覆盖查看交互执行顺序

1. 先补 smoke 断言，锁定 `Domain chip` 和推荐卡次按钮行为。
2. 在 `InterviewsPage.tsx` 里复用现有筛选状态，实现领域导航过滤与推荐卡次按钮。
3. 调整 CSS，补激活态和推荐卡按钮布局。
4. 更新 feature / architecture 文档。
5. 跑 lint / build / smoke。
